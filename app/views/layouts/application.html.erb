<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>NF Emissor</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- ✅ Bootstrap 5.3.3 -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous">

    <!-- ✅ Font Awesome 6.6.0 (Free) -->
    <!-- ✅ Font Awesome 5.15.4 (Free, estável e garantido) -->
<link
rel="stylesheet"
href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"
integrity="sha512-dyZt2msH1I9cw4Zaxk7+eR0D6UoQ2j9bJ3CmwXrbpYuU4eApGZzR+YtLks2A1Ngl5Fp9zOKfFeFjXZoUj1kR7Q=="
crossorigin="anonymous"
referrerpolicy="no-referrer"
/>


    <!-- ✅ App CSS -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- ✅ Importmap / JS -->
    <%= javascript_importmap_tags %>

    <!-- ✅ Bootstrap JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ENjdO4Dr2bkBIFxQpeo5s6VKKj3M9xCkYkU5Lr9q+Y1p8nEG1GZC+t+Uc2x0xIM+"
      crossorigin="anonymous"></script>

    <style>
      /* 🎨 Estilos da sidebar e hover dos ícones */
      nav {
        background-color: #1c1f23;
      }

      .nav-link {
        border-radius: 8px;
        transition: all 0.2s ease-in-out;
      }

      .nav-link:hover {
        background-color: rgba(255, 255, 255, 0.1);
        transform: translateX(3px);
      }

      .nav-link i {
        transition: color 0.2s ease-in-out, transform 0.2s ease-in-out;
      }

      .nav-link:hover i {
        color: #ffc107;
        transform: scale(1.1);
      }

      .nav-link.disabled {
        cursor: not-allowed;
        opacity: 0.5;
      }

      h5 i {
        animation: pulse 2s infinite;
      }

      @keyframes pulse {
        0% { color: #ffc107; }
        50% { color: #f8f9fa; }
        100% { color: #ffc107; }
      }
    </style>
  </head>

  <body class="bg-light">
    <% if user_signed_in? %>
      <div class="d-flex">
        <nav class="bg-dark text-white p-3 d-flex flex-column" style="min-height: 100vh; width: 240px;">
          <h5 class="mb-4 text-center">
            <i class="fas fa-file-invoice-dollar text-warning mr-2"></i> NF Emissor
          </h5>
        
          <ul class="nav flex-column gap-2">
            <li class="nav-item">
              <%= link_to authenticated_root_path, class: "nav-link text-white d-flex align-items-center" do %>
                <i class="fas fa-chart-line mr-2"></i> Dashboard
              <% end %>
            </li>
        
            <li class="nav-item">
              <% if current_user.certificate_installed? %>
                <%= link_to new_nota_fiscal_path, class: "nav-link text-white d-flex align-items-center" do %>
                  <i class="fas fa-file-medical mr-2"></i> Emitir Nota
                <% end %>
              <% else %>
                <a class="nav-link disabled text-secondary d-flex align-items-center">
                  <i class="fas fa-file-medical mr-2"></i> Emitir Nota
                </a>
              <% end %>
            </li>
        
            <li class="nav-item">
              <% if current_user.certificate_installed? %>
                <%= link_to notas_fiscais_path, class: "nav-link text-white d-flex align-items-center" do %>
                  <i class="fas fa-folder-open mr-2"></i> Notas Emitidas
                <% end %>
              <% else %>
                <a class="nav-link disabled text-secondary d-flex align-items-center">
                  <i class="fas fa-folder-open mr-2"></i> Notas Emitidas
                </a>
              <% end %>
            </li>
        
            <li class="nav-item">
              <%= link_to new_certificate_path, class: "nav-link text-white d-flex align-items-center" do %>
                <i class="fas fa-shield-alt mr-2"></i> Certificado
              <% end %>
            </li>
        
            <li class="nav-item mt-4">
              <%= link_to destroy_user_session_path,
                          data: { turbo_method: :delete },
                          class: "nav-link text-danger d-flex align-items-center" do %>
                <i class="fas fa-sign-out-alt mr-2"></i> Sair
              <% end %>
            </li>
          </ul>
        </nav>
        

        <main class="flex-grow-1 p-4">
          <% if notice %>
            <div class="alert alert-success"><%= notice %></div>
          <% end %>
          <% if alert %>
            <div class="alert alert-danger"><%= alert %></div>
          <% end %>

          <% if user_signed_in? && !current_user.certificate_installed? %>
            <div class="alert alert-warning d-flex justify-content-between align-items-center">
              <div>
                <strong>Certificado obrigatório!</strong>
                Configure o certificado digital para emitir e visualizar notas.
              </div>
              <%= link_to "Configurar", new_certificate_path, class: "btn btn-sm btn-primary" %>
            </div>
          <% end %>

          <%= yield %>
        </main>
      </div>
    <% else %>
      <div class="container mt-5">
        <%= yield %>
      </div>
    <% end %>
  </body>
</html>
